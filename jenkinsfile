pipeline {
    agent any
    
    tools {
        maven 'Projeckt-do-it-now-Pipeline
'
    }

    environment {
        SONAR_AUTH_TOKEN = credentials('Projeckt-do-it-now-Pipeline
') // Corrected usage of environment variable
    }
    
    stages {
        stage('Cloning') {
            steps {
                git branch: 'main', changelog: false, poll: false, url: 'https://github.com/DEL-ORG/Projeckt-do-it-now-Pipeline.git'
'
            }
        }

        stage('Maven Package') {
            steps {
                sh 'mvn clean package -Dmaven.repo.local=/tmp/target'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                script {
                    docker.image('sonarsource/sonar-scanner-cli:latest').inside {
                        sh '''
                            sonar-scanner \
                                -Dsonar.projectKey=Projeckt-do-it-now-Pipeline
 \
                                -Dsonar.projectName=Projeckt-do-it-now-Pipeline
 \
                                -Dsonar.host.url=https://sonarqube.devopseasylearning.uk \
                                -Dsonar.login=$SONAR_AUTH_TOKEN
                        '''
                    }
                }
            }
        }
    }
}